<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('partials/theme-head') %>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= formatPageTitle(titulo || "Confirmación de cuenta") %></title>
  <link rel="stylesheet" href="/css/intranet.css">
</head>
<body style="background-color: #f5f7fa;">

  <div class="login-page">
    <div class="card-login">
      <h2>Confirmación de cuenta</h2>

      <% if (ok) { %>
        <div class="mensaje-exito" style="text-align: center; font-size: 1.1rem;">
          <%= message %>
        </div>
        <div style="text-align: center; margin-top: 20px;">
          <a href="/login" class="btn btn-primario">Ir a Iniciar Sesión</a>
        </div>
      <% } else { %>
        <div class="mensaje-error" style="text-align: center;">
          <%= message %>
        </div>
        <div style="text-align: center; margin-top: 20px;">
          <a href="/register" style="color: #003a70; font-weight: bold;">Volver a registrarse</a>
        </div>
      <% } %>

    </div>
  </div>

</body>
</html>